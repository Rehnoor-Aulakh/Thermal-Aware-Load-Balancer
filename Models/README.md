# 🔥 Thermal-Aware CPU Temperature Prediction using LSTM

A deep learning project for forecasting CPU temperature from real-time hardware telemetry collected across multiple laptops. The objective is to predict future CPU temperatures using historical system metrics, enabling proactive thermal management and intelligent scheduling for thermal-aware load balancing systems.

---


# 📌 Latest Updates

- Added the latest cleaned datasets from five different laptops.
- Removed older datasets and deprecated models.
- Added the latest CNN + GRU and CNN + LSTM architectures.
- Updated feature set with additional CPU, GPU, memory, SSD, and system telemetry.
- Exported the highest-performing CNN + GRU model (R² = **0.8921**) to ONNX.
- Updated the scaler used by the Load Balancer inference pipeline.
- Improved cross-device generalization using telemetry from five hardware configurations.

# 📑 Model Summary

| Property | Value |
|----------|-------|
| Model | Bidirectional LSTM |
| Framework | TensorFlow / Keras |
| Input Sequence Length | 20 timesteps |
| Prediction Target | Next CPU Temperature |
| Features | 7 Hardware Telemetry Features |
| Optimizer | Adam |
| Loss Function | Mean Squared Error (MSE) |
| Evaluation Metrics | MAE, RMSE, R² |
| Train/Validation/Test Split | 80% / 10% / 10% |
| Hardware Used | NVIDIA RTX 4060 Laptop GPU |

---
# 📑 Models Implemented

The repository currently contains the latest deep learning models for CPU temperature prediction.

| Model | Description |
|--------|-------------|
| CNN + GRU | Convolutional feature extraction followed by a GRU for temporal sequence modeling |
| CNN + LSTM | Convolutional feature extraction followed by an LSTM for sequence prediction |

Older experimental models have been removed from the repository and replaced with these latest architectures.

# 📂 Datasets

The latest version of the project uses telemetry collected from **five different laptops**, providing improved hardware diversity and better cross-device generalization.

| Dataset | Samples |
|---------|--------:|
| Manan | 15,275 |
| Prabhsimrat | 14,291 |
| Sushant | 14,133 |
| Gursimar | 14,621 |
| Gurvivek | 15,391 |

**Total telemetry samples:** **73,711**

The datasets are generated by merging hardware telemetry logs with stress event annotations. Older datasets have been removed, and only the latest cleaned datasets are included in this repository.


# 📊 Model Comparison

| Model | Datasets Used | MAE (°C) | RMSE (°C) | R² Score |
|--------|---------------|---------:|----------:|---------:|
| **CNN + GRU** | Prabhsimrat, Sushant, Manan, Gursimar, Gurvivek | **2.203** | **4.809** | **0.8921** |
| **CNN + LSTM** | Prabhsimrat, Sushant, Manan, Gursimar, Gurvivek | **2.210** | **4.825** | **0.8914** |

# 📈 Persistence Baseline Comparison

The persistence baseline predicts the next CPU temperature by assuming that the next reading is equal to the current temperature. It serves as a simple benchmark for evaluating the effectiveness of the deep learning models.

| Model | Datasets Used | MAE (°C) | RMSE (°C) | R² Score |
|--------|---------------|---------:|----------:|---------:|
| **Persistence Baseline** | Prabhsimrat, Sushant, Manan, Gursimar, Gurvivek | **2.795** | **5.648** | **0.8512** |

### Comparison with Deep Learning Models

| Model | MAE (°C) | RMSE (°C) | R² Score |
|--------|---------:|----------:|---------:|
| **Persistence Baseline** | 2.795 | 5.648 | 0.8512 |
| **CNN + GRU** | **2.203** | **4.809** | **0.8921** |
| **CNN + LSTM** | **2.210** | **4.825** | **0.8914** |

The latest deep learning models significantly outperform the persistence baseline across all evaluation metrics, demonstrating improved accuracy and stronger generalization on the combined five-device telemetry dataset.

### 📌 Observation

Across all experiments, the persistence baseline remains a strong benchmark for short-term (one-step) CPU temperature prediction due to the high temporal correlation between consecutive telemetry samples. While several deep learning models outperform the baseline, comparing against persistence provides a consistent reference for evaluating improvements across different architectures and dataset combinations.


# 📊 Features Used

The latest models are trained using a richer set of hardware telemetry collected every few seconds.

The dataset includes the following features:

- Timestamp
- CPU Usage
- CPU Temperature
- CPU Package Power
- CPU Core Voltage
- CPU Average Clock
- CPU Efficiency Core Average Clock
- CPU Factors
- Virtual Memory Usage
- Total Memory Usage
- GPU Temperature
- GPU Hotspot Temperature
- GPU Core Load
- GPU Core Voltage
- GPU Package Power
- GPU Core Clock
- GPU Memory Clock
- GPU Memory Load
- SSD Composite Temperature
- Network Connections
- Process Count

These features provide a more comprehensive representation of system behavior, enabling improved thermal prediction across different hardware platforms.

# 🧠 Model Architecture

```
Input Sequence (20 × 7)

        │

Bidirectional LSTM
Hidden Size = 64
2 Layers

        │

Dropout (0.1)

        │

Fully Connected Layer

        │

Predicted CPU Temperature
```

---

# ⚙️ Training Configuration

| Hyperparameter | Value |
|---------------|-------|
| Hidden Size | 64 |
| LSTM Layers | 2 |
| Bidirectional | Yes |
| Dropout | 0.10 |
| Batch Size | 32 |
| Lookback Window | 20 |
| Learning Rate | 0.00779 |
| Weight Decay | 1.125×10⁻⁶ |
| Optimizer | Adam |
| Scheduler | ReduceLROnPlateau |
| Early Stopping | Enabled |

---

# 📈 Training Performance

The training and validation losses converged smoothly without significant divergence, indicating stable optimization.

Final Training Loss

```
0.004851
```

Final Validation Loss

```
0.003789
```

Validation Gap

```
21.9%
```

This indicates only **mild overfitting**, suggesting that the model generalizes reasonably well while maintaining low prediction error.

---



# 🏆 Best Performing Model

| Model | MAE (°C) | RMSE (°C) | R² Score |
|--------|---------:|----------:|---------:|
| **CNN + GRU** | **2.203** | **4.809** | **0.8921** |

The CNN + GRU model achieved the highest prediction accuracy and has been exported as the production ONNX model used by the Thermal-Aware Load Balancer.

# 🚀 Future Work

Future improvements include:

- Multi-step CPU temperature forecasting
- Prediction horizons of 30–60 seconds
- Transformer-based sequence models
- Additional hardware telemetry features
- Cross-device domain adaptation
- Online continual learning
- Integration with the Thermal-Aware Load Balancer

---

# 📌 Conclusion

This repository presents the latest version of the Thermal-Aware CPU Temperature Prediction pipeline using hardware telemetry collected from five different laptops.

The project now focuses on two production-ready hybrid deep learning architectures: **CNN + GRU** and **CNN + LSTM**.

Among the evaluated models, **CNN + GRU** achieved the best overall performance:

- **MAE:** 2.203 °C
- **RMSE:** 4.809 °C
- **R² Score:** 0.8921

The best-performing model has been exported to ONNX format and integrated into the Thermal-Aware Load Balancer, enabling real-time CPU temperature prediction for intelligent thermal-aware scheduling.